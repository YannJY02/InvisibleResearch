interface:
  display_name: "InvisibleResearch"
  short_description: "Repo-specific patterns and workflows for InvisibleResearch"
  default_prompt: "Use the InvisibleResearch repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true